Steps to utilise the files:

1. run download_dataset.py
2. run the cell in main*.ipynb that processes raw data into processed data & splits it into Testing/Training set. (all of this in 1 cell)

main - shows the entire journey of testing a classical ML model vs DL model
main_polised* - a quick run for random case prediction + simulation (C-ML m.)

Notes:
Run each cell 1 by 1.
Simulations are heavy, delete prev as you regenerate (del ~)

